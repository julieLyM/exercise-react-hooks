import React from 'react';

export const Counter = () => {
  const context = useContext(contextValue);
  useEffect(() => {
    effect;
    return () => {
      cleanup;
    };
  }, []);
  return <div></div>;
};
